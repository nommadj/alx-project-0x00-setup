export interface CardProps {
  title: string;
  imageUrl: string;
  rating: number;
}

export interface PillProps {
  title: string;
}

export interface ButtonProps {
  title: string;
  styles: string;
}